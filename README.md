# lego-bin-labels

Generate small monochrome bitmaps of LEGO parts (rendered from LDraw via LDView) for printing bin labels on a Brother P-touch (LBX) printer.

See `docs/superpowers/specs/` for the design.

# brick-icons — shading/rendering test specimens (LDraw part ids)
# The parts we render-check when iterating on shading, scale, and HLR.
# Chosen to exercise studs, curves, gradients, and irregular geometry.
# One id per line; whole-line '#' comments and blank lines ignored.

# --- Basics (studs, flat faces, plates vs bricks) ---
3005    # 1x1 brick — single stud, box faces
3024    # 1x1 plate — short, one stud
3001    # 2x4 brick — many studs
3020    # 2x4 plate
3040b   # 45 slope 2x1 — sloped flat face

# --- Round / cylinder gradients ---
3941    # 2x2 round brick — 4 studs + gradient body + center tube (gate case)
6143    # 2x2 round brick reinforced — round body variant
50950   # curved slope 3x1 — smooth curved top
4589    # cone 1x1 — tapered wall
3960    # dish 4x4 inverted — large shallow curve

# --- SNOT (studs not on top) ---
4070    # headlight brick 1x1 — recessed side stud + notch
87087   # 1x1 brick with stud on 1 side
99781   # bracket 1x2-1x2 down — thin L profile, studs on two planes

# --- Irregular / weird behavior ---
4019    # Technic gear 16 tooth — many small tooth faces + axle hole
3649    # Technic gear 40 tooth — dense teeth (stress case)
6589    # Technic gear 12 tooth bevel — angled teeth
3673    # Technic pin — small round connector
32062   # Technic axle 2 notched — cross-section extrusion

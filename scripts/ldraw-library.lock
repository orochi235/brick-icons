# Known-working LDraw parts library snapshot (vendor/ldraw is gitignored).
# complete.zip from library.ldraw.org is a ROLLING snapshot — the URL in
# setup-ldview.sh always serves the latest parts update, so a fresh setup
# can silently pull different geometry than the renders were tuned against.
# This file records the fingerprint of the copy this repo is calibrated to.
#
# Verify a local copy:
#   cd vendor/ldraw && find p parts -name "*.dat" -type f | LC_ALL=C sort \
#     | xargs shasum -a 256 | shasum -a 256
source: https://library.ldraw.org/library/updates/complete.zip
fetched: 2026-06-27
dat-files: 36067
manifest-sha256: 87641c77573ea3ff91d0bb477799659c48ec06d0ab2166153fa727322d56aea2
